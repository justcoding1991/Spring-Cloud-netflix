spring:
  cloud:
    config:
      discovery:
        service-id: spring-cloud-config-server
        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka

